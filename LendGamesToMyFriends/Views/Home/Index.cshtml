@{
    ViewBag.Title = "Home Page";
}

@if (!string.IsNullOrEmpty(ViewBag.Error))
{
    <div class="alert alert-danger" role="alert">@ViewBag.Error</div>
}

@using (Html.BeginForm("Login", "Home", FormMethod.Post, new { @class = "form-signin" }))
{
    <h2 class="form-signin-heading">Entrar no Sistema</h2>
    <div class="form-group">
        @Html.Label("email", "Seu E-mail", new { @class = "sr-only" })
        @Html.TextBox("email", null, new { @class = "form-control", type = "email", required = "required", placeholder = "Email" })
    </div>
    <div class="form-group">
        @Html.Label("password", "Sua Senha", new { @class = "sr-only" })
        @Html.TextBox("password", null, new { @class = "form-control", type = "password", required = "required", placeholder = "Email" })
    </div>
    <div class="form-group">
        <button class="btn btn-lg btn-primary btn-block" type="submit">Entrar</button>
        @Html.ActionLink("Ainda não sou registrado", "Register", "Home")
    </div>

}
